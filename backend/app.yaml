runtime: nodejs18

env: standard

instance_class: F1

env_variables:
  NODE_ENV: 'production'
  PORT: '8080'
  MONGODB_URI: 'YOUR_MONGODB_URI_HERE'

handlers:
- url: /.*
  script: auto
  secure: always

automatic_scaling:
  min_instances: 0
  max_instances: 1
  target_cpu_utilization: 0.65
